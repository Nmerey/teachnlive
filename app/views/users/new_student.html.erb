<%= form_for(url: '/create_student') do |s| %>
	<div class="field">
		<%= s.email_field :email ,:class => "text-center" ,placeholder: "Email"%>
	</div>
	<button type="submit">Create</button>
<% end %>